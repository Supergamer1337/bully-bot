{
	"name": "anti-cortexus-bot",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"compile": "tsc --build --watch",
		"watch": "nodemon ./dist/index.js",
		"clear-dist": "rm -r dist/",
		"build-docker": "pnpm clear-dist && tsc --build && sudo docker build -t supergamer1337/bully-bot ."
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"nodemon": "^2.0.12",
		"typescript": "^4.3.5"
	},
	"dependencies": {
		"discord.js": "^13.0.1",
		"dotenv": "^10.0.0"
	}
}
